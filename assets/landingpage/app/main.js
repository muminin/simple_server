var body_load = false;

function base_url(name) {
      return url+name;
}

var aplikasi = angular.module('pusadata', ['ngRoute','ngAnimate','amChartsDirective','ui.select2','ui.bootstrap'])

aplikasi.config(function($routeProvider,$locationProvider) {
	
	// route for the home page
})

aplikasi.run(function($rootScope) {
   //$rootScope.tahun = 	thn_data ;
});


$(window).resize(function(){
     // This will report the width of your div in px.
});

